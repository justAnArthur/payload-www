// core/blocks is empty — RenderBlocks moved to render/blocks. The
// directory is kept here only so historical import paths
// (`@justanarthur/payload-www/core/blocks`) still resolve to a
// barrel; new code should import from `render/blocks`.
export {}
