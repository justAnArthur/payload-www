import { appearanceOptions, link, type LinkAppearances, type LinkOptions } from '../collections/fields/link'
import { linkGroup } from '../collections/fields/linkGroup'
import { slugField } from '../collections/fields/slug'

const fields = { link, linkGroup, appearanceOptions, slugField }

export default fields
export { appearanceOptions, link, linkGroup, slugField, type LinkAppearances, type LinkOptions }
