import { anyone, authenticated, authenticatedOrPublished } from '../access/index'

export default { anyone, authenticated, authenticatedOrPublished }

export { anyone, authenticated, authenticatedOrPublished } from '../access/index'
