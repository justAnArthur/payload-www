import type { GoogleResolverConfig } from '../../resolvers/google'
import { googleResolver } from '../../resolvers/google'

export default googleResolver

export { googleResolver }
export type { GoogleResolverConfig }
